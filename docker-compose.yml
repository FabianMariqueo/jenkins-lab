version: '3.9'
services:
  jenkins:
    container_name: jenkins
    image: jenkins/jenkins
    ports:
      - "8080:8080"
    volumes:
      - $PWD/jenkins_home:/var/jenkins_home
      - ./scripts:/home/scripts
    networks:
      - net
networks:
  net: